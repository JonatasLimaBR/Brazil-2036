dataset_id: pib_mensal
br2036_domain: macro
br2036_module: M04

catalog_url: "https://api.bcb.gov.br/dados/serie/bcdata.sgs.4380/dados"
organization: "Banco Central do Brasil (BCB)"
license: "Dados abertos do governo federal"

metric_ids:
  - pib_mensal
unit: BRL
allow_negative_metric_ids: []

contract_path: contracts/pib_mensal.yaml

gcp_project: ""
raw_bucket: ""
raw_prefix: macro

bq_dataset_control: br2036_control
bq_dataset_bronze: br2036_bronze
bq_dataset_silver: br2036_silver
bq_dataset_gold: br2036_gold

bronze_table: pib_mensal_raw
bronze_columns:
  - reference_period
  - value
field_delimiter: ","

silver_model: pib_mensal
gold_model: gold_pib_mensal

# Unused for this source (BCB SGS JSON API, no CKAN) -- WideSeriesConfig
# requires these fields but the run script never calls list_resources() for
# this dataset. Kept empty rather than generalizing the shared config loader
# for one non-CKAN source (YAGNI).
ckan_base_url: ""
ckan_package_id: ""
