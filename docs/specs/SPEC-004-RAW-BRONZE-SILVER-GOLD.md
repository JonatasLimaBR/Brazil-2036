# SPEC-004 — Data Layers
RAW is immutable and stores source bytes plus manifest.
Bronze represents source-shaped ingestion.
Silver standardizes types, dates, territorial keys and code sets.
Gold exposes canonical data products.
Breaking schema drift MUST be quarantined before Silver.
