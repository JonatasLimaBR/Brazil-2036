# SPEC-006 — Data Trust Score
Score dimensions: freshness, completeness, availability, documentation, consistency.
Store dimension scores and aggregate score; never store only aggregate.
Production model inputs below configured threshold require explicit override with audit.
