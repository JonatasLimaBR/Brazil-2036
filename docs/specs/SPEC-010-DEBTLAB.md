# SPEC-010 — DebtLab
Input: base debt/GDP, real/nominal rates as selected methodology, growth, primary balance, horizon and scenario id.
Core dynamics follow registered formula/version.
Output includes yearly trajectory, assumptions, model version and classification SIMULATED.
No LLM arithmetic path may replace the engine.
