# SPEC-020 — Agent Capability Registry
Capabilities are explicit allowlists.
Read agents do not register write tools.
Policy agent may create draft scenario but not publish.
Authorization is checked again server-side even when tool is present.
Capability registry is versioned and auditable.
