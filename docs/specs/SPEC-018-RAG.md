# SPEC-018 — RAG
Documents are parsed, normalized, chunked, embedded and indexed with metadata.
Retrieval combines lexical/vector where supported plus filters by organization/type/date/legal reference.
Answers cite retrieved source metadata.
If evidence is insufficient, answer must say evidence was insufficient.
